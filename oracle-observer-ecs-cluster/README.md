# Oracle Observer ECS Cluster
This cluster is used for running Oracle data guard Observers.   We keep it separate from the Shared ECS Cluster used for Delius as it is ring-fenced
for Oracle-specific tasks.